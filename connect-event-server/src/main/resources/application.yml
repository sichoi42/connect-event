spring:
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:mariadb://localhost/connectevent?serverTimezone=UTC}
    username: connectevent
    password: password
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        id:
          new_generator_mappings: true
